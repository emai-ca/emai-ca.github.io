source "https://rubygems.org"

# Matches the build environment GitHub Pages uses, so a local preview
# behaves the same as the deployed site.
gem "github-pages", group: :jekyll_plugins

# Needed on Ruby 3.4+ where these left the standard library.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"

gem "webrick"
